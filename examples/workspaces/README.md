# Example Workspaces

These are lightweight scenario starters for ProductOS V12. They are intentionally small and repo-safe: each workspace captures a distinct product context, workspace identity, and operating mode without claiming private dogfood proof.

Use them as references for naming, segmentation, and mission framing. The bundled regression fixture remains under `tests/fixtures/workspaces/productos-sample/`.
